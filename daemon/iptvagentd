#! /bin/bash

# source cfg file
. /usr/local/IPTVAgent/cfg/iptvagent.cfg

python $daemonPath/control/server.py > /dev/null 2>&1 &

#continuously call the realtime shell script
while true 
do
    sleep $realtimeInterval
    $modulePath/realtime > /dev/null 2>&1 &
done

